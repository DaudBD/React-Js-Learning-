const Hero = (props) => {
  return (
    <div>
      <button onClick={props.childClickBtn}>Submit</button>
    </div>
  );
};

export default Hero;


// const Hero = (props) => {
//     return (
//         <div>
     
     
//      <h1> {props.title}</h1>

//      <p>{props.des}</p>
//         </div>
//     );
// };

// export default Hero;